// Creación de clases en JavaScript
function Account(name, document/*Al colocar las variables aqui, creamos el método constructor*/) {
    this.id; // Atributos de la clase
    this.name = name; // Atributos de la clase, Asigna directamente las variables locales a las globales
    this.document = document; // Atributos de la clase, Asigna directamente las variables locales a las globales
    this.email; // Atributos de la clase
    this.password; // Atributos de la clase

}